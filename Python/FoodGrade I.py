'''FoodGrade I'''
def process1(check):
    '''input + หาเลขในช่วงที่กำหนด'''
    chiken = float(input())
    if 50 <= chiken and chiken <= 70:
        check = check + 1
    return check

def fgi():
    '''หาเลขตามข้อกำหนด'''
    check = 0
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    check = process1(check)
    print(check)
fgi()
